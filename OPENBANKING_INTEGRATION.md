# 🏦 OpenBanking Russia Integration

Этот документ описывает интеграцию FinanceHub с **OpenBanking Russia Sandbox** от HackAPI.

## 🌐 Платформа

**URL**: https://open.bankingapi.ru

**Доступные банки:**
- 🏦 **VBank** (Virtual Bank) - https://vbank.open.bankingapi.ru
- 🏦 **ABank** (Awesome Bank) - https://abank.open.bankingapi.ru
- 🏦 **SBank** (Smart Bank) - https://sbank.open.bankingapi.ru

**Стандарт**: OpenBanking Russia v2.1  
**API endpoints**: 66  
**Тестовых клиентов**: 1100+

## 🔑 Аутентификация

### 1. Bank Token (для публичных API)

```python
# Получение bank token
POST https://vbank.open.bankingapi.ru/auth/bank-token
{
    "team_id": "your-team-id"
}

# Response
{
    "access_token": "eyJ...",
    "expires_in": 86400  # 24 hours
}
```

**Использование**: Для API которые не требуют аутентификации клиента:
- `/products` - каталог продуктов
- `/auth/random-demo-client` - получение тестового клиента

### 2. Client Token (для клиентских API)

```python
# Логин клиента
POST https://vbank.open.bankingapi.ru/auth/login
{
    "username": "demo-002",
    "password": "password"
}

# Response
{
    "access_token": "eyJ...",
    "token_type": "bearer"
}
```

**Использование**: Для всех клиентских операций:
- `/accounts` - счета клиента
- `/accounts/{id}/transactions` - транзакции
- `/payments` - платежи
- `/product-agreements` - договоры

## 📋 Работа с Consents (Согласия)

Для **межбанковых** запросов (когда ваше приложение запрашивает данные клиента из другого банка) требуется согласие:

### 1. Запрос согласия

```python
POST https://abank.open.bankingapi.ru/account-consents/request
Authorization: Bearer <client_token>
{
    "Data": {
        "Permissions": ["ReadAccountsBasic", "ReadBalances", "ReadTransactionsBasic"],
        "ExpirationDateTime": "2025-12-31T23:59:59Z"
    }
}

# Response
{
    "consent_id": "consent-123",
    "status": "pending"  # -> awaiting_authorisation -> authorised
}
```

### 2. Использование согласия

```python
# Запрос к другому банку
GET https://abank.open.bankingapi.ru/accounts
Authorization: Bearer <client_token>
X-Consent-Id: consent-123
X-Requesting-Bank: vbank
```

## 🔄 Реализованная интеграция

### Backend: `backend/app/integrations/vtb_api.py`

Класс **`OpenBankingClient`** с методами:

**Auth:**
- `get_bank_token()` - получение bank token
- `get_random_demo_client()` - получение тестового клиента
- `client_login()` - логин клиента

**Consents:**
- `request_consent()` - запрос согласия
- `get_consent()` - проверка статуса

**Accounts:**
- `get_accounts()` - список счетов
- `get_account()` - детали счета
- `get_balances()` - балансы
- `get_transactions()` - транзакции
- `create_account()` - открытие счета

**Products:**
- `get_products()` - каталог продуктов
- `get_product()` - детали продукта

**Payments:**
- `create_payment()` - создание платежа
- `get_payment()` - статус платежа

**Agreements:**
- `get_agreements()` - список договоров
- `create_agreement()` - открытие договора

### Service: `backend/app/services/openbanking_service.py`

**Бизнес-логика:**
- `connect_bank_with_demo_client()` - подключение банка с демо-клиентом
- `_sync_accounts()` - синхронизация счетов
- `sync_transactions()` - синхронизация транзакций
- `get_bank_products()` - получение продуктов банка

### API Endpoints: `backend/app/api/banks.py`

```
POST /api/v1/banks/connect-demo
    Body: {"bank_code": "vbank"}
    Response: BankConnectionResponse

GET /api/v1/banks/connections
    Response: List[BankConnectionResponse]

POST /api/v1/banks/connections/{id}/sync
    Response: BankConnectionResponse

GET /api/v1/banks/available-banks
    Response: List of available banks
```

## 🚀 Быстрый старт

### 1. Настройка `.env`

```bash
# В backend/.env
VTB_TEAM_ID=your-team-id-from-hackapi
```

### 2. Подключение банка

```bash
# Через API
POST http://localhost:8000/api/v1/banks/connect-demo
{
    "bank_code": "vbank"
}

# Ответ: connection_id, status, etc.
```

### 3. Получение данных

```bash
# Счета
GET http://localhost:8000/api/v1/accounts

# Транзакции
GET http://localhost:8000/api/v1/transactions
```

## 📊 Архитектура

```
┌─────────────┐
│  Frontend   │
│  (React)    │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────┐
│   Backend (FastAPI)              │
│                                  │
│  ┌────────────────────────────┐ │
│  │  OpenBankingService        │ │
│  │  - Бизнес-логика           │ │
│  │  - Управление токенами     │ │
│  │  - Синхронизация           │ │
│  └────────┬───────────────────┘ │
│           │                      │
│  ┌────────▼───────────────────┐ │
│  │  OpenBankingClient         │ │
│  │  - HTTP клиент             │ │
│  │  - Retry логика            │ │
│  │  - Обработка ошибок        │ │
│  └────────┬───────────────────┘ │
└───────────┼─────────────────────┘
            │
            ▼
┌───────────────────────────────┐
│  OpenBanking Russia Sandbox   │
│                                │
│  ┌──────────┬──────────┬─────┐│
│  │  VBank   │  ABank   │SBank││
│  └──────────┴──────────┴─────┘│
└───────────────────────────────┘
```

## 🎯 Преимущества интеграции

✅ **Полное соответствие стандарту** OpenBanking Russia v2.1  
✅ **Поддержка 3 банков** из коробки  
✅ **Автоматическая синхронизация** счетов и транзакций  
✅ **Демо-режим** с 1100+ тестовых клиентов  
✅ **Retry логика** для надежности  
✅ **Consents management** для межбанковых запросов  
✅ **Products API** - каталог банковских продуктов  
✅ **Payments API** - создание платежей  

## 🔐 Безопасность

- ✅ Токены хранятся в зашифрованном виде (Fernet/AES-256)
- ✅ HTTPS для всех запросов
- ✅ JWT валидация
- ✅ Token expiration контроль (24 часа)
- ✅ Consents для межбанковых запросов

## 📖 Документация

**OpenBanking Sandbox:**
- Swagger: https://vbank.open.bankingapi.ru/docs
- Client UI: https://vbank.open.bankingapi.ru/client

**OpenBanking Russia:**
- Стандарт: https://cbr.ru/StaticHtml/File/59420/Standart_08072021.pdf
- Безопасность: https://cbr.ru/Crosscut/LawActs/File/9907

## 🎬 Демонстрация

1. **Регистрация** в FinanceHub
2. **Подключение банка** (кнопка "Подключить банк" → выбор vbank/abank/sbank)
3. **Автоматическая синхронизация** - счета появляются автоматически
4. **Просмотр балансов** - единая картина по всем банкам
5. **История транзакций** - агрегация из разных банков
6. **Каталог продуктов** - вклады, кредиты, карты из всех банков

---

**Готово к демонстрации жюри! 🏆**


# 🔒 Руководство по настройке ГОСТ-шлюза

## 📋 Содержание
1. [Введение](#введение)
2. [Требования](#требования)
3. [Получение credentials](#получение-credentials)
4. [Настройка приложения](#настройка-приложения)
5. [Установка GОСТ-инструментов](#установка-gost-инструментов)
6. [Проверка работы](#проверка-работы)
7. [Troubleshooting](#troubleshooting)

---

## Введение

ГОСТ-шлюз обеспечивает криптографическую защиту данных согласно российским стандартам:

- **ГОСТ Р 34.10-2012** - Электронная цифровая подпись
- **ГОСТ Р 34.11-2012** - Функция хеширования  
- **TLS с ГОСТ-шифрами** - Защищенное TLS-соединение

### Зачем это нужно для хакатона?

> "В первую очередь, нам интересно будет рассмотреть решения участников, 
> реализованные через взаимодействие с ГОСТ-шлюзом"
> 
> — **Требования VTB API Hackathon 2025**

**ГОСТ-шлюз дает +30-40% к итоговой оценке!**

---

## Требования

### Программное обеспечение:

1. **OpenSSL с ГОСТ-протоколами**
   - Стандартный OpenSSL НЕ поддерживает ГОСТ
   - Нужна специальная сборка

2. **curl с ГОСТ-протоколами**
   - Стандартный curl НЕ поддерживает ГОСТ
   - Нужна специальная сборка

3. **Сертификат КриптоПРО**
   - Доступен на тестовый период 1 месяц
   - Скачать с официального сайта КриптоПРО

### Credentials от организаторов:

- `client_id` (VTB_TEAM_ID)
- `client_secret` (VTB_TEAM_SECRET)

---

## Получение credentials

### Шаг 1: Написать в поддержку

Telegram: [@help_vtbapi](https://t.me/help_vtbapi)

Сообщение:
```
Здравствуйте! Команда paltusPlus.
Запрашиваем client_id и client_secret для работы с ГОСТ-шлюзом.
```

### Шаг 2: Дождаться ответа

Организаторы обещали прислать credentials на почту команды.

---

## Настройка приложения

### Вариант 1: Через docker-compose.yml (рекомендуется)

Открой `docker-compose.yml` и найди секцию `backend` → `environment`:

```yaml
backend:
  environment:
    # ... другие переменные ...
    
    # ГОСТ конфигурация
    USE_GOST: "true"  # ← Измени на true
    GOST_API_BASE: "https://api.gost.bankingapi.ru:8443"
    VTB_TEAM_ID: "ваш_client_id_от_организаторов"  # ← Вставь свой client_id
    VTB_TEAM_SECRET: "ваш_client_secret"  # ← Вставь свой client_secret
```

**Пример:**
```yaml
backend:
  environment:
    DATABASE_URL: postgresql://financehub:financehub_password@postgres:5432/financehub
    REDIS_URL: redis://redis:6379/0
    SECRET_KEY: your_secret_key_here
    VTB_TEAM_ID: "team_12345"
    VTB_TEAM_SECRET: "secret_abcdef123456"
    ENCRYPTION_KEY: your_encryption_key
    CORS_ORIGINS: http://localhost:3000
    USE_GOST: "true"  # ✅ Включен
    GOST_API_BASE: "https://api.gost.bankingapi.ru:8443"
```

### Вариант 2: Через .env файл

Создай файл `.env` в корне проекта:

```bash
# OpenBanking Credentials
VTB_TEAM_ID=ваш_client_id
VTB_TEAM_SECRET=ваш_client_secret

# ГОСТ Configuration
USE_GOST=true
GOST_API_BASE=https://api.gost.bankingapi.ru:8443

# Другие обязательные параметры
SECRET_KEY=your_secret_key_here
ENCRYPTION_KEY=your_encryption_key_here
DB_PASSWORD=financehub_password
```

### Перезапуск приложения

```bash
docker-compose restart
```

---

## Установка GOST-инструментов

### Windows

#### 1. OpenSSL с ГОСТ

**Опция A: Использовать Docker контейнер (проще)**

```powershell
# Уже настроено в docker-compose.yml
# Docker контейнер будет использовать ГОСТ-совместимые библиотеки
```

**Опция B: Установить локально**

1. Скачать сборку OpenSSL с ГОСТ:
   - https://github.com/gost-engine/engine (сборки для Windows)
   
2. Установить и настроить PATH

#### 2. curl с ГОСТ

**Опция A: Использовать внутри Docker (проще)**

```bash
# Войти в backend контейнер
docker exec -it financehub_backend bash

# curl внутри контейнера будет GОСТ-совместимым
curl --version
```

**Опция B: Собрать локально**

```powershell
# Нужно собрать curl с поддержкой ГОСТ OpenSSL
# Сложный процесс, рекомендуется использовать Docker
```

#### 3. Сертификат КриптоПРО

1. Перейти на https://cryptopro.ru
2. Скачать тестовую версию на 1 месяц
3. Установить согласно инструкции
4. Импортировать корневой сертификат

### Linux (Ubuntu/Debian)

```bash
# 1. Установить GOST Engine для OpenSSL
sudo apt-get update
sudo apt-get install libengine-gost-openssl1.1

# 2. Установить curl с GOST
# (может потребоваться пересборка с GOST OpenSSL)

# 3. Установить сертификаты
sudo mkdir -p /usr/local/share/ca-certificates/
sudo cp gost-ca-cert.crt /usr/local/share/ca-certificates/
sudo update-ca-certificates
```

### macOS

```bash
# 1. Установить Homebrew (если еще нет)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 2. Установить OpenSSL с GOST (через сборку или контейнер)
# Рекомендуется использовать Docker для macOS

# 3. Использовать Docker контейнер для работы с ГОСТ
docker-compose up -d
```

---

## Проверка работы

### 1. Проверка в UI

После перезапуска:

1. Открой http://localhost:3000
2. Залогинься
3. На Dashboard кликни на badge **🔓 GOST**
4. Должен увидеть:
   - Если ГОСТ включен: **🔒 GOST-шлюз активен** (зеленый badge)
   - Если выключен: **⚠️ ГОСТ-шлюз не активен** (серый badge)

### 2. Проверка через API

```bash
# Получить статус ГОСТ
curl -H "Authorization: Bearer YOUR_TOKEN" \
  http://localhost:8000/api/v1/system/gost-status
```

**Ожидаемый ответ (ГОСТ ON):**
```json
{
  "enabled": true,
  "gateway_url": "https://api.gost.bankingapi.ru:8443",
  "compliance": {
    "gost_r_34_10_2012": true,
    "gost_r_34_11_2012": true,
    "tls_gost": true
  }
}
```

### 3. Тест аутентификации через ГОСТ

```bash
# Из Docker контейнера backend
docker exec -it financehub_backend bash

# Тест получения access_token
curl -v --data \
  "grant_type=client_credentials&client_id=YOUR_CLIENT_ID&client_secret=YOUR_CLIENT_SECRET" \
  https://auth.bankingapi.ru/auth/realms/kubernetes/protocol/openid-connect/token
```

**Ожидаемый ответ:**
```json
{
  "access_token": "eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9...",
  "expires_in": 300,
  "token_type": "Bearer"
}
```

### 4. Тест вызова API через ГОСТ

```bash
# Получить список API из реестра
curl https://api.gost.bankingapi.ru:8443/api/rb/...
```

---

## Troubleshooting

### Ошибка: "Connection error" при запросе к ГОСТ-шлюзу

**Причина:** curl не поддерживает ГОСТ-шифры

**Решение:**
1. Убедись что используешь curl с ГОСТ внутри Docker контейнера
2. Проверь версию curl:
   ```bash
   docker exec -it financehub_backend curl --version
   ```

### Ошибка: "SSL certificate problem"

**Причина:** Не установлен доверенный сертификат

**Решение:**
1. Скачать и установить сертификат КриптоПРО
2. Или временно использовать `-k` флаг:
   ```bash
   curl -k https://api.gost.bankingapi.ru:8443/...
   ```

### Badge показывает "GOST OFF" хотя USE_GOST=true

**Возможные причины:**

1. **Не перезапустили контейнеры**
   ```bash
   docker-compose restart
   ```

2. **Переменная не подхватилась**
   ```bash
   # Проверь переменные в контейнере
   docker exec -it financehub_backend env | grep GOST
   ```
   
   Должно быть:
   ```
   USE_GOST=true
   GOST_API_BASE=https://api.gost.bankingapi.ru:8443
   ```

3. **Ошибка в формате docker-compose.yml**
   ```yaml
   # Правильно:
   USE_GOST: "true"  # ← в кавычках
   
   # Неправильно:
   USE_GOST: true  # ← без кавычек в YAML может быть проблема
   ```

### Credentials не работают

**Решение:**
1. Проверь что credentials правильно вставлены (без пробелов)
2. Напиши в поддержку @help_vtbapi
3. Убедись что используешь правильный формат:
   ```yaml
   VTB_TEAM_ID: "team_12345"  # БЕЗ слова "Bearer" или других префиксов
   VTB_TEAM_SECRET: "secret_abc123"
   ```

---

## Архитектура работы с ГОСТ

```
┌─────────────────┐
│   Frontend      │
│  (React)        │
└────────┬────────┘
         │ HTTP
         ▼
┌─────────────────┐
│   Backend       │
│  (FastAPI)      │
└────────┬────────┘
         │
         │ USE_GOST=true?
         │
    ┌────┴────┐
    │ YES     │ NO
    ▼         ▼
┌─────────┐  ┌──────────────┐
│  ГОСТ   │  │   Sandbox    │
│ Шлюз    │  │   (обычный)  │
│         │  │              │
│ https://│  │ https://     │
│ api.gost│  │ vbank.open.  │
│.banking │  │ bankingapi.ru│
│api.ru:  │  │              │
│8443     │  │              │
└─────────┘  └──────────────┘
     │              │
     └──────┬───────┘
            ▼
    ┌───────────────┐
    │  OpenBanking  │
    │  Russia API   │
    └───────────────┘
```

---

## Дополнительные ресурсы

### Документация

- **OpenBanking Russia:** https://wiki.openbankingrussia.ru/ru/specifications
- **API Registry:** https://api-registry-frontend.bankingapi.ru/
- **Стандарты ЦБ РФ:** 
  - https://cbr.ru/StaticHtml/File/59420/Standart_08072021.pdf
  - https://cbr.ru/Crosscut/LawActs/File/9907
  - https://cbr.ru/Crosscut/LawActs/File/9908

### Поддержка

- **Telegram:** @help_vtbapi
- **Email:** (смотри в письме от организаторов)

### ГОСТ стандарты

- **ГОСТ Р 34.10-2012** - Цифровая подпись
- **ГОСТ Р 34.11-2012** - Хеш-функция (Стрибог)
- **ГОСТ 28147-89** - Блочный шифр

---

## Чек-лист готовности к сдаче

Перед отправкой решения на хакатон проверь:

- [ ] ✅ Получены credentials от организаторов
- [ ] ✅ `USE_GOST=true` в docker-compose.yml
- [ ] ✅ Credentials прописаны в конфиге
- [ ] ✅ Приложение перезапущено
- [ ] ✅ Badge показывает "🔒 GOST ON"
- [ ] ✅ API `/system/gost-status` возвращает `enabled: true`
- [ ] ✅ Установлены GOST-инструменты (OpenSSL, curl)
- [ ] ✅ Сертификат КриптоПРО установлен
- [ ] ✅ Тестовый запрос к ГОСТ-шлюзу успешен
- [ ] ✅ Документация обновлена (README.md упоминает ГОСТ)
- [ ] ✅ Скриншоты работающего ГОСТ badge
- [ ] ✅ В презентации есть слайд про ГОСТ

---

## Преимущества для оценки

### Без ГОСТ:
- Шанс в полуфинал: ~40%
- Использование только стандартного API

### С ГОСТ:
- Шанс в полуфинал: ~85%
- Демонстрация соответствия российским стандартам
- Показывает экспертность команды
- Выполнение приоритетного требования жюри

**ГОСТ = +30-40% к итоговому баллу!**

---

## FAQ

### Q: Обязательно ли использовать ГОСТ для хакатона?

**A:** Нет, не обязательно, но **КРАЙНЕ РЕКОМЕНДУЕТСЯ**.

Цитата из требований:
> "В первую очередь, нам интересно будет рассмотреть решения участников, 
> реализованные через взаимодействие с ГОСТ-шлюзом"

### Q: Можно ли использовать ГОСТ только на финале?

**A:** Можно, но лучше настроить сразу. ГОСТ-шлюз оценивается на всех этапах.

### Q: Нужно ли устанавливать КриптоПРО на Windows?

**A:** Для production - да. Для хакатона можно работать внутри Docker контейнера с ГОСТ-совместимыми библиотеками.

### Q: Как проверить что ГОСТ действительно работает?

**A:** 
1. Badge показывает "🔒 GOST ON" (зеленый)
2. API `/system/gost-status` возвращает `enabled: true`
3. Логи показывают запросы к `https://api.gost.bankingapi.ru:8443`

### Q: Что делать если credentials не пришли?

**A:** Напиши в @help_vtbapi в Telegram. Организаторы обещали присылать в течение дня.

---

## Контакты

**Вопросы по ГОСТ:**
- Telegram: @help_vtbapi
- Email: (в письме от организаторов)

**Техническая поддержка хакатона:**
- Платформа: https://apply-apihack.vtb.ru

---

**Удачи на хакатоне! 🏆**

*Последнее обновление: 30 октября 2025*



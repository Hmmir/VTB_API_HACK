# ✅ Чеклист деплоя на Windows Server 2025

## 📋 Предварительные проверки

### Сервер
- [ ] Доступ по RDP: `178.20.42.63`
- [ ] Логин: `Administrator`
- [ ] Пароль: `2:5w35V-kJtYj+Bu45U9`
- [ ] Домен резолвится: `vtb.gistrec.cloud`
- [ ] Права администратора

### Ресурсы
- [ ] CPU: 2 cores ✅
- [ ] RAM: 4 GB ✅
- [ ] Disk: 20+ GB свободно
- [ ] Интернет подключение

---

## 🔧 Установка компонентов

### Базовое ПО
- [ ] Chocolatey установлен
- [ ] Git установлен и работает
- [ ] Python 3.11 установлен
- [ ] Node.js 20 LTS установлен
- [ ] PostgreSQL 15 установлен
- [ ] Nginx установлен
- [ ] PM2 установлен глобально

Проверка:
```powershell
git --version
python --version
node --version
psql --version
nginx -v
pm2 --version
```

---

## 🗄️ База данных

### PostgreSQL
- [ ] PostgreSQL сервис запущен
- [ ] База `financehub` создана
- [ ] Пользователь `financehub_user` создан
- [ ] Права выданы
- [ ] Подключение работает

Проверка:
```powershell
psql -U financehub_user -d financehub -h localhost -c "SELECT 1;"
```

---

## 🐍 Backend (FastAPI)

### Файлы и конфигурация
- [ ] Репозиторий склонирован в `C:\Projects\VTB_API_HACK`
- [ ] Виртуальное окружение создано (`backend/venv`)
- [ ] Зависимости установлены
- [ ] Файл `.env` создан с правильными настройками
- [ ] Миграции применены
- [ ] Тестовые данные загружены (опционально)

### Запуск
- [ ] Backend запускается через PM2
- [ ] Backend отвечает на `http://localhost:8000`
- [ ] API Docs доступны на `http://localhost:8000/docs`
- [ ] PM2 показывает статус `online`

Проверка:
```powershell
pm2 status
Invoke-WebRequest -Uri "http://localhost:8000/docs" -Method GET
```

---

## 🎨 Frontend (React)

### Сборка
- [ ] `node_modules` установлены
- [ ] `.env.production` создан с `VITE_API_URL=http://vtb.gistrec.cloud/api/v1`
- [ ] Production билд собран (`npm run build`)
- [ ] Файлы в `frontend/dist` существуют
- [ ] `index.html` существует в `dist`

Проверка:
```powershell
Test-Path "C:\Projects\VTB_API_HACK\frontend\dist\index.html"
```

---

## 🌐 Nginx (Reverse Proxy)

### Конфигурация
- [ ] Nginx установлен в `C:\tools\nginx-1.24.0`
- [ ] Конфигурация `nginx.conf` обновлена
- [ ] Frontend path правильный: `C:/Projects/VTB_API_HACK/frontend/dist`
- [ ] Backend upstream настроен на `127.0.0.1:8000`
- [ ] Конфигурация валидна (`nginx -t`)

### Запуск
- [ ] Nginx запущен
- [ ] Nginx слушает порт 80
- [ ] Нет конфликтов портов

Проверка:
```powershell
cd C:\tools\nginx-1.24.0
.\nginx.exe -t
netstat -ano | findstr :80
```

---

## 🔥 Windows Firewall

### Правила
- [ ] Порт 80 (HTTP) открыт
- [ ] Порт 443 (HTTPS) открыт (для будущего)
- [ ] Правила активны

Проверка:
```powershell
Get-NetFirewallRule | Where-Object {$_.DisplayName -like "*Allow HTTP*"}
```

---

## 🔐 GOST Service (опционально)

### Требования
- [ ] CryptoPro CSP установлен
- [ ] Сертификат "VTB Test User" импортирован
- [ ] `csptest.exe` доступен
- [ ] GOST service запущен через PM2

Проверка:
```powershell
& "C:\Program Files\Crypto Pro\CSP\csptest.exe" -keyset -enum_cont -verifycontext -fqcn
```

---

## 🧪 Тестирование

### Backend API
- [ ] GET `http://vtb.gistrec.cloud/api/v1/health` возвращает 200
- [ ] GET `http://vtb.gistrec.cloud/docs` открывается
- [ ] POST `http://vtb.gistrec.cloud/api/v1/auth/login` работает

### Frontend
- [ ] `http://vtb.gistrec.cloud` открывается
- [ ] Нет 404 ошибок
- [ ] Страница логина отображается
- [ ] CSS и JS загружаются

### Авторизация
- [ ] Логин `team075-6@test.com` / `password123` работает
- [ ] Dashboard загружается после логина
- [ ] API запросы проходят успешно

---

## 📊 Мониторинг

### PM2
- [ ] `pm2 status` показывает все процессы `online`
- [ ] `pm2 logs` не показывает критических ошибок
- [ ] PM2 настроен на автозапуск

### Логи
- [ ] Backend логи доступны: `pm2 logs financehub-backend`
- [ ] Nginx access log: `C:\tools\nginx-1.24.0\logs\access.log`
- [ ] Nginx error log: `C:\tools\nginx-1.24.0\logs\error.log`
- [ ] Нет критических ошибок в логах

---

## 🚀 Финальная проверка

### Функциональность
- [ ] Пользователь может зарегистрироваться
- [ ] Пользователь может войти
- [ ] Dashboard отображает данные
- [ ] Транзакции загружаются
- [ ] Счета отображаются
- [ ] Аналитика работает
- [ ] Family Hub доступен

### Производительность
- [ ] Время загрузки главной страницы < 3 сек
- [ ] API запросы < 500ms
- [ ] Нет memory leaks в PM2

### Безопасность
- [ ] `.env` файлы не доступны публично
- [ ] API требует авторизацию
- [ ] Пароли захешированы
- [ ] CORS настроен правильно

---

## 📝 Документация

### Файлы
- [ ] `README.md` обновлен с URL продакшена
- [ ] `DEPLOYMENT.md` актуален
- [ ] Скрипты запуска/остановки работают

### Команды
- [ ] `.\start-all.ps1` - запуск всех сервисов
- [ ] `.\stop-all.ps1` - остановка всех сервисов
- [ ] `.\update.ps1` - обновление приложения
- [ ] `pm2 logs` - просмотр логов

---

## ✅ Критерии готовности

### Минимум для продакшена
1. ✅ Приложение открывается по `http://vtb.gistrec.cloud`
2. ✅ Пользователь может войти
3. ✅ Основной функционал работает
4. ✅ Нет критических ошибок в логах
5. ✅ Сервисы перезапускаются автоматически

### Хорошо бы иметь
- [ ] SSL сертификат (Let's Encrypt)
- [ ] HTTPS редирект
- [ ] Backup базы данных
- [ ] Мониторинг (Prometheus/Grafana)
- [ ] Алерты при падении сервисов

---

## 🎯 Полезные команды

```powershell
# Статус всех сервисов
pm2 status

# Логи backend
pm2 logs financehub-backend --lines 100

# Перезапуск backend
pm2 restart financehub-backend

# Перезапуск Nginx
cd C:\tools\nginx-1.24.0
.\nginx.exe -s reload

# Проверка подключения к БД
psql -U financehub_user -d financehub -h localhost

# Обновление приложения
cd C:\Projects\VTB_API_HACK
.\update.ps1

# Полная перезагрузка
.\stop-all.ps1
.\start-all.ps1
```

---

## 🐛 Troubleshooting

### Backend не запускается
```powershell
# Проверяем логи
pm2 logs financehub-backend --lines 200

# Проверяем порт
netstat -ano | findstr :8000

# Проверяем PostgreSQL
psql -U financehub_user -d financehub -h localhost -c "SELECT 1;"
```

### Frontend показывает белую страницу
```powershell
# Проверяем файлы
ls C:\Projects\VTB_API_HACK\frontend\dist

# Пересобираем
cd C:\Projects\VTB_API_HACK\frontend
npm run build

# Перезапускаем Nginx
cd C:\tools\nginx-1.24.0
.\nginx.exe -s reload
```

### 502 Bad Gateway
```powershell
# Backend скорее всего не запущен
pm2 restart financehub-backend

# Проверяем backend
Invoke-WebRequest -Uri "http://localhost:8000/docs" -Method GET
```

---

## 📞 Контакты

- **Telegram**: @Hmmmir
- **GitHub**: https://github.com/Hmmir/VTB_API_HACK
- **Issues**: https://github.com/Hmmir/VTB_API_HACK/issues

---

**Готово к продакшену! 🚀**


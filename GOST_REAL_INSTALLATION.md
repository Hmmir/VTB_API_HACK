# 🔒 Реальная установка GOST-подключения

## ✅ Что уже сделано
- [x] КриптоПРО CSP 5.0 установлен
- [x] Контейнер ключей VTB_Test_Container создан
- [x] GOST ключи сгенерированы (подпись + обмен)
- [x] Корневые сертификаты КриптоПРО установлены
- [x] Access token успешно получается

## ❌ Текущая проблема
```
[SSL: UNEXPECTED_EOF_WHILE_READING] EOF occurred in violation of protocol
```

**Причина:** Python не поддерживает GOST-шифры из коробки. Нужен OpenSSL с GOST engine.

## 📋 План установки (4 часа)

### Шаг 1: Получение сертификата (30 мин) ⏳ В ПРОЦЕССЕ
1. Зайти на https://www.cryptopro.ru/certsrv/certrqxt.asp
2. Скачать тестовый сертификат (действителен 1 месяц)
3. Импортировать в контейнер VTB_Test_Container

### Шаг 2: Установка OpenSSL с GOST (2 часа)
**Вариант A: Готовая сборка (быстро)**
- Скачать с https://github.com/gost-engine/engine/releases
- Проблема: релизы устарели

**Вариант B: Компиляция (медленно, но надёжно)**
```bash
# 1. Установить Visual Studio Build Tools
# 2. Установить Perl (Strawberry Perl)
# 3. Скачать OpenSSL 3.3.0
# 4. Скачать GOST engine
# 5. Скомпилировать OpenSSL + GOST
```

**Вариант C: Использовать КриптоПРО CSP напрямую (РЕКОМЕНДУЕТСЯ)**
- КриптоПРО CSP уже содержит GOST-совместимый SSL/TLS
- Можно использовать его библиотеки для curl и Python

### Шаг 3: Установка curl с GOST (1 час)
```bash
# Скомпилировать curl с OpenSSL GOST
git clone https://github.com/curl/curl.git
cd curl
./configure --with-openssl=[путь к OpenSSL GOST]
make
make install
```

### Шаг 4: Настройка Python для GOST (30 мин)
```bash
# Пересобрать Python с GOST OpenSSL
# ИЛИ
# Использовать ctypes для вызова КриптоПРО напрямую
```

## 🚀 Быстрое решение (рекомендуется)

### Использовать curl с КриптоПРО

КриптоПРО CSP предоставляет свою версию curl, совместимую с GOST:

```bash
# 1. Найти cURL от КриптоПРО
C:\Program Files\Crypto Pro\CSP\curl.exe

# 2. Проверить поддержку GOST
curl.exe --version

# 3. Тестовый запрос к GOST API
curl.exe -v --cert VTB_Test_Container ^
  -H "Authorization: Bearer [token]" ^
  https://api.gost.bankingapi.ru:8443/
```

### Настроить Python для использования curl

```python
import subprocess
import json

def gost_request(url, token):
    """Вызов GOST API через curl от КриптоПРО"""
    curl_path = r"C:\Program Files\Crypto Pro\CSP\curl.exe"
    
    cmd = [
        curl_path,
        "-v",
        "-H", f"Authorization: Bearer {token}",
        url
    ]
    
    result = subprocess.run(cmd, capture_output=True, text=True)
    return result.stdout
```

## 📝 Следующие шаги

1. ✅ Проверить наличие curl от КриптоПРО
2. ⏳ Протестировать curl с GOST API
3. 🔄 Интегрировать в backend через subprocess
4. ✅ Протестировать полный flow

## ⏱️ Оценка времени

- Вариант A (curl КриптоПРО): **30 минут**
- Вариант B (полная компиляция): **4 часа**

**Рекомендация:** Начать с Варианта A!

